server:
  port: 8081
  
spring:
  application:
    name: simple-consumer

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:8761/eureka
  instance:
    prefer-ip-address: true